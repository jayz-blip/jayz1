# 환경 변수 설정 확인 및 수정

## ✅ 확인 사항

### 1. 유형 (Type)
- ✅ **"텍스트"** 선택 - **올바름!**
- `BACKEND_URL`은 일반 텍스트 값이므로 "텍스트" 유형이 맞습니다.

### 2. 변수 이름 (Variable Name)
- ✅ **`BACKEND_URL`** - **올바름!**
- 프론트엔드 코드에서 사용하는 변수 이름과 일치합니다.

### 3. 값 (Value) ⚠️
- ❌ **`https://chatbot-api.jayz-407.workers.dev/`** - **문제 있음!**
- 마지막에 `/`가 있습니다. 이것을 제거해야 합니다.

## 🔧 수정 방법

### 올바른 값:
```
https://chatbot-api.jayz-407.workers.dev
```

### 잘못된 값:
```
https://chatbot-api.jayz-407.workers.dev/  ← 마지막에 / 있음
```

## 📋 수정 단계

1. **값 필드에서 마지막 `/` 제거**
   - 현재: `https://chatbot-api.jayz-407.workers.dev/`
   - 수정: `https://chatbot-api.jayz-407.workers.dev`

2. **"저장" 버튼 클릭**

3. **재배포 대기**
   - "이 변경 내용은 다음 배포에 적용됩니다." 메시지가 표시됨
   - 자동으로 재배포되거나 수동으로 재배포 필요

## ⚠️ 왜 마지막 `/`를 제거해야 하나요?

Pages Functions 코드에서 다음과 같이 URL을 구성합니다:

```javascript
const apiUrl = `${backendUrl}/api${path}${url.search}`;
```

만약 `backendUrl`이 `https://chatbot-api.jayz-407.workers.dev/`로 끝나면:
- 결과: `https://chatbot-api.jayz-407.workers.dev//api/chat` (이중 슬래시)

올바른 `backendUrl`이 `https://chatbot-api.jayz-407.workers.dev`이면:
- 결과: `https://chatbot-api.jayz-407.workers.dev/api/chat` (정상)

## ✅ 최종 확인

환경 변수 설정:
- **유형**: 텍스트 ✅
- **변수 이름**: BACKEND_URL ✅
- **값**: `https://chatbot-api.jayz-407.workers.dev` (마지막 `/` 제거) ✅

---

**값에서 마지막 `/`를 제거하고 저장하세요!**

