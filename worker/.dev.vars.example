# 로컬 개발용 환경 변수
# 이 파일을 .dev.vars로 복사하고 실제 값 입력
# .dev.vars 파일은 Git에 포함되지 않습니다 (.gitignore)

# OpenAI API 키 (선택사항)
# Cloudflare Workers 대시보드에서 환경 변수로 설정하는 것을 권장합니다
OPENAI_API_KEY=your_openai_api_key_here

