{
  "name": "chatbot-worker",
  "version": "1.0.0",
  "description": "Cloudflare Workers Python 백엔드",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:local": "wrangler d1 execute chatbot-db --local",
    "d1:prod": "wrangler d1 execute chatbot-db"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}

